version: "2"
services:
    docker-sshd:
        from: centos/systemd:latest
        roles:
            - sshd
        ports:
            - "2222:22"
        volumes:
            - "/sys/fs/cgroup:/sys/fs/cgroup:ro"

registries: {}
