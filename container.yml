version: "2"
services:
    docker-sshd:
        from: centos/systemd:latest
        roles:
            - sshd
        ports:
            - "22"
        volumes:
            - "/sys/fs/cgroup"
        entrypoint:
            - ["/usr/sbin/init"]

registries: {}
