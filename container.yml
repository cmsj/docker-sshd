version: "2"
services:
    ssh:
        from: centos/systemd:latest
        roles:
            - sshd
        ports:
            - 22
        volumes:
            - "/sys/fs/cgroup:/sys/fs/cgroup:ro"

registries: {}
